// Admin coverage target:
// - /admin/produtos exposes product rows and side drawer actions.
// - /admin/precos exposes price-change fields and history/audit copy.
// - /api/admin/publish requires authorization, validates payload and revalidates public routes.
// - /api/admin/preview-share creates noindex, revocable, read-only preview links.
