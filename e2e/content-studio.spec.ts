// Content Studio coverage target:
// - /admin/conteudo renders editor, limits and preview modes.
// - /api/admin/publish rejects unauthenticated requests and revalidates authorized publication payloads.
// - /admin/preview and /admin/preview/share/[token] remain noindex and read-only.
